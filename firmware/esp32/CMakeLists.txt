cmake_minimum_required(VERSION 3.5)

# Inclui as ferramentas do ESP-IDF
include($ENV{IDF_PATH}/tools/cmake/project.cmake)

# Define o nome do projeto (pode ser qualquer coisa)
project(esp32_robot_firmware)